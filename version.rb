# frozen_string_literal: true

# Version information for SonVu CNC Plugins. Keep release metadata here so the
# extension version can be reused by features and future packaging tasks.

module SonVu
  module CNCPlugins
    VERSION = '0.16.0'
  end
end
